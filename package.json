{
  "name": "drink-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && git -C dist init && git -C dist add -A && git -C dist commit -m 'Deploy' && git -C dist push -f https://github.com/Andrei-Piticas/Liquido2.0.git HEAD:gh-pages"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "fast-check": "^3.15.0",
    "happy-dom": "^12.10.3"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
